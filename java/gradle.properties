# version is overridden at build time via -Pversion=<spec version without leading v>
version = 0.0.0

org.gradle.parallel = true
# Build cache can be disabled with --no-build-cache option
org.gradle.caching = true
